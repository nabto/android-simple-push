package com.nabto.simplepush.edge

class DeviceManager {
    suspend fun updateTokenOnDevices(token : String) {
        // get a list of all paired devices
        // connect and update the token on each device.
        // if an update fail, mark the device as not configured properly.

    }
}